/**
 * Icon names drawn by the app itself rather than taken from the Material Icons font. Menus carry
 * them in the same `icon` field as a font ligature; the prefix keeps them from ever matching one.
 */
export const HAND_CARDS_ICON = 'axe:hand-cards';
