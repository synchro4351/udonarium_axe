# 保存と読み込み

ルームの状態を書き出して保存したり、保存データを読み込んで復元したりできます。

<Shot src="room-snapshot" alt="自動保存パネル。世代数と保存操作のボタンがある" width="520" caption="自動保存された世代はこのブラウザの中だけに残ります" />

## 保存

FAB メニューの **「保存」** を押すと、ルーム全体が zip ファイルとして書き出されます。
テーブル・コマ・カード・チャットなどの状態がまとめて含まれます。

## 自動保存

テーブルに変更があると、しばらく操作が落ち着いたタイミングで**ルームのスナップショットが自動で保存**されます。
ブラウザが落ちたりタブを閉じたりしても、次に開いたときに続きから再開できます。

- 変更が **20 秒**止んだら保存。操作が続く場合も **3 分**に一度は必ず保存します
- **コマをドラッグしている間は保存しません**。手を離してブラウザの手が空いたタイミングまで待ちます
- 画像が多くて保存に時間がかかるルームでは、**保存の間隔を自動で広げます**（前回の保存が 1 秒を超えたら 3 倍、3 秒を超えたら 6 倍。最大で「2 分止んだら保存・18 分に一度は必ず保存」）
- 保存するのは **GM / PL（テーブルを編集できるロール）** だけです。見学のブラウザには保存されません
- 保存先は**そのブラウザの中だけ**で、他の参加者には共有されません
- 直近 **5 世代**、合計 **256MB** まで保持し、超えた分は古いものから自動で削除されます
- 中身は「保存」で書き出す zip と同じ（テーブル・コマ・カード・チャット・画像）です

### 前回の卓を復元する

ルームに接続していない状態でアプリを開くと、保存されたスナップショットがある場合に画面上部へ
**「前回の卓データがこのブラウザに残っています」** と表示されます。**復元する** を押すとその状態が読み込まれます。
不要なら **あとで** で閉じられます。

[部屋設定](/manual/room-settings#自動保存)の **自動保存** からは、保存された世代の一覧を開けます。

- 世代ごとに日時・ルーム名・サイズを表示し、選んで **復元**／**削除**
- **今すぐ保存** で任意のタイミングのスナップショットを追加
- **すべて削除** で保存済みのスナップショットを破棄

::: warning ブラウザの中だけの保存です
スナップショットはブラウザのストレージ（IndexedDB）に置かれます。別の端末やブラウザからは参照できず、
閲覧データを消去すると一緒に消えます。セッションを確実に残したいときは、これとは別に
**「保存」で zip を手元に書き出して**ください。
:::

::: tip 大きなルームでも操作は止まりません
保存データの書き出し・読み込みは、圧縮も展開も**バックグラウンドで処理**します。画像が数百枚ある
数十 MB のルームでも、保存中・読み込み中に画面が固まりにくくなっています。
:::

## ZIP 読込

FAB メニューの **「ZIP 読込」** から、書き出した zip や、コマ・コマ集の xml を読み込んでルームに反映できます。
配布されたキャラクターデータ（zip / xml）を取り込むときにも使います。
スマートフォンでは[メニューの **「読込」**](/manual/mobile#メニューから開けるもの)が同じ働きをします。

zip はテーブルに**ドラッグ&ドロップ**しても読み込めます。中に入っている画像はメディアに登録されるだけで、コマにはなりません
（コマを立てたいときは[画像そのものを落として](/manual/character#画像を落としてコマを作る)ください）。

ココフォリアのルームデータ zip を落としたときだけは、この読み込みではなく
[ココフォリアのルーム取り込み](/manual/room-import)（実験的機能）として扱われます。

::: tip 読み込み時のオーナーについて
保存データには各オブジェクトの[オーナー](/manual/disclosure#オーナー)（当時の参加者）が記録されていますが、**読み込み時にオーナーは解除されます**。別セッションの持ち主情報が残って視界や操作が誰にも紐づかなくなるのを防ぐためです。読み込み後は、各自がコマやメモのオーナーを取り直してください。
:::

## 個別の書き出し

- キャラクターは、シートの保存からコマ単位で zip 書き出しできます
- チャットログは [チャットの基本](/manual/chat) の HTML ログ書き出しを利用します

> ゲームデータは P2P で共有される一方、中央サーバーには保存されません。
> 自動保存は各自のブラウザ内の保険なので、セッションを残したいときは「保存」で手元に書き出しておくと安心です。
