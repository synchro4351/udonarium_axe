# 行動順

戦闘などで「今は誰の手番か」を管理するための機能です。テーブル上のキャラクターを並び順どおりに表示し、手番が変わるたびにチャットへ通知して、対象のコマを光らせます。手番の状態は参加者全員で同期されます。

<Shot src="turn-order" alt="行動順ウィジェット。キャラクターのアイコンが順に積み重なっている" width="560" caption="ラウンド表示にすると行動順ウィジェットに切り替わります" />

## 行動順ウィジェットを開く

[インベントリ](/manual/inventory) を開き、タイトルバーのボタンで **ラウンド表示** にすると、インベントリが **行動順ウィジェット** に切り替わります。キャラクターのアイコンが行動順に積み重なって表示され、現在の手番のキャラクターがリングと淡い光で強調されます。

- 上部のうっすら色の付いたバーをドラッグして、好きな位置へ動かせます
- 元のインベントリに戻すには、同じボタンをもう一度押します（リッチ表示に戻ります）
- 表示順は、インベントリ「テーブル」タブの **並び順** がそのまま使われます（既定は敏捷度の降順、同値なら名前順）。並び順の変え方は [インベントリ](/manual/inventory) を参照してください
- 手番を回しながら全員の HP や状態異常も見たいときは [簡略表示](/manual/inventory#簡略表示は-1-体-1-行の表)にします。1 体 1 行の表になり、行動順のマーカーも各行の左に出ます

## ツールバーでの表示

戦闘中は、[GM ツールバー](/manual/roles#gm-ツールバー) と [PL ツールバー](/manual/pl-tools) にも **ラウンド数と現在の手番** が出ます。ウィジェットを開いていなくても、今が誰の番かを常に確認できます。戦闘が始まっていないあいだは表示されません。

GM ツールバーには前後の送りボタンも付いているので、進行役はウィジェットを開かずに手番を回せます。

## 手番を進める

ウィジェット上部のボタンで手番を操作します。手番が確定するたびに、対象のコマがテーブル上で光り、チャットにシステムメッセージが流れます。

- **次へ** — 手番を渡します
- **前へ** — 1 つ前の状態へ戻します
- **⏩ ラウンドを進める** — 今のラウンドを閉じて、次を開きます
- **⏪ ラウンドを戻す** — 前のラウンドが終わった時点まで戻します
- アイコンを **クリック** — そのキャラクターを直接、現在の手番にします
- **リセット** — 手番とラウンドを最初の状態へ戻します

### 済んだ人は飛ばす

**次へ**を押すと、そのとき手番だったキャラクターが**行動済み**になり、まだ行動していない中でいちばん早い人へ手番が渡ります。行動済みのコマには一覧やアイコンに ✓ が付きます。

割り込みで誰かを先に動かしたいときは、アイコンをクリックしてその人を手番にしてください。**次へ**を押せば、飛ばした人たちの中から順番どおりに続きます。全員が済むとラウンドが終わります。

行動済みは、次のラウンドが**開始**した時点で解除されます。

### まだ動いていない人がいるとラウンドは進みません

**⏩ ラウンドを進める** を押したとき、まだ手番を取っていないコマが残っていると、名前を並べた確認が出ます。押し忘れなのか、意図して飛ばすのかはこちらでは分からないので、判断はあなたに返します。

そのまま進めれば、残っていたコマは行動しないまま次のラウンドに入ります。取り消せば、ラウンドはその場に留まります。名前が多いときは先頭の何体かだけを挙げて、残りは数で示します。

**次へ** を押し続けて全員が済んだ場合は、確認は出ません。

## 陣営ごとに動かす

味方が全員動いてから敵が全員動く、という進み方にもできます。[部屋設定](/manual/room-settings#ラウンドは-1-体ずつか、陣営ごとか) の **進行のしかた** を「陣営ごと」にすると切り替わります。

陣営は [パーティ](/manual/party) がそのまま使われます。パーティに入れていないコマは「無所属」としてひとまとめになります。回る順番と、無所属を行動させるかどうかも部屋設定で決めます。

行動順ウィジェットは、陣営ごとに区切って並びます。パーティの名前と色の見出しが付き、いまフェイズが来ている陣営の見出しが光ります。**リッチ表示と簡略表示（表）の一覧にも同じ見出しが入り**、どのコマがどの陣営かが開いたままでも分かります。GM・PL ツールバーと配信オーバーレイのラウンド表示にも、陣営の名前が並んで出ます。

一覧の見出しが出るのは卓のタブだけで、フォルダ表示や検索中は出ません。フォルダも検索も、それ自体がコマを束ねる仕組みだからです。その上から陣営でも束ねると、同じ問いに答えが 2 つ並びます。

進め方はフェイズ内の設定で変わります。

- **自由** — フェイズが開いても手番は誰にも渡りません。その陣営の中なら誰から動かしてもかまわず、コマをクリックするとその手番になって、直前まで動いていたコマは行動済みになります
- **並び順どおり** — フェイズが開いた時点で先頭のコマに手番が渡り、並び順に 1 体ずつ回ります

**次へ** の働きはどちらも同じです。その陣営のまだ動いていないコマへ手番が渡り、全員が動き終えて初めて次の陣営のフェイズが開きます。1 回押しただけで相手のフェイズに移ることはありません。

**前へ** と **⏪ ラウンドを戻す** は陣営ごとでもそのまま使えます。どの陣営のフェイズだったかも一緒に戻ります。

バフの残ラウンドが減るのは、これまでと同じ場所です。手番の開始と終了はコマごとに、ラウンド終了は最後の陣営のフェイズが閉じたあとに来ます。陣営が交代した時点では減りません。

メンバー全員が手番を持たない陣営は、フェイズを開かずに飛ばします。

### 手番を持たないコマ

背景に置いた馬車や、倒れて動かない敵のように、順番を回す必要のないコマがあります。コマを右クリックして **☐ 手番をもたない** を選ぶと、そのコマは行動順から外れます。

インベントリには今までどおり並びます。バフの残ラウンドも他のコマと同じように減ります。順番だけを飛ばします。

「次へ」を押していくと、ラウンドの区切りも含めて次のように進みます。

1. **ラウンド X 開始です。**
2. **〇〇 の行動です。**（行動順に各キャラクター）
3. （最後のキャラクターまで繰り返し）
4. **ラウンド X 終了です。**
5. **ラウンド X+1 開始です。**（次のラウンドへ）

## バフの残ラウンドを自動で減らす

**ラウンドが終わるたびに、テーブル上の全員のバフ／デバフの残ラウンドが 1 ずつ減ります。**
0 になったものは自動で消え、**誰の何が切れたか**がチャットに流れます。

`3R` で付けたバフはちょうど 3 ラウンド持ちます。`&R-` や `&D` を毎ラウンド打つ必要はありません。

- **前へ**は、進めたときに起きたことをそのまま巻き戻します。**切れて消えたバフも戻ります**（残ラウンド・メモ・色・並び順まで）。⏪ ラウンドを戻すなら、前のラウンドが終わった時点まで一息に戻せます
- 手番を進めた人のブラウザだけが処理するので、二重に減ることはありません
- 行動順のボタン列にある**砂時計アイコン**で、この自動減少を切り替えられます（卓全体の設定として共有されます）
- **手番で切れるバフ**は、このラウンド終了の一斉減少には含まれません。指定したキャラクターの手番が始まる
  （または終わる）ときに、そのバフだけが減ります。設定は[バフ／デバフ](/manual/buff#いつ切れるかを決める)を参照してください

::: tip 手動で操作したいとき
砂時計アイコンをオフにすると、従来どおりバフは動かなくなります。
`&R-`（全部 1 減らす）・`&D`（0 以下を消す）や[リモートコントローラ](/manual/pl-tools)のボタンで、
好きなタイミングだけ進められます。記法は[チャットの特殊記法](/manual/chat-syntax#バフ-デバフ操作)を参照してください。
:::

## インベントリ画面からの操作

行動順は、リッチ表示や簡略表示のままインベントリ「テーブル」タブからも操作できます。

- 一覧のすぐ上に **ラウンド表示・前へ・次へ・ラウンド送り・リセット** が並びます
- 現在の手番のキャラクターには行の先頭に ▶ が、行動済みのキャラクターには ✓ が付きます
- 各キャラクターの行のボタンから、そのキャラクターを直接 **「手番にする」** こともできます

手番の通知は [チャット](/manual/chat) のシステムメッセージとして参加者全員へ届きます。
